# Python virtual environment
.venv/
__pycache__/
*.pyc

# Local Database
sleep_tracker.db

# Dynamic runtime files (Gist にバックアップ、ローカルのみ)
legacy/src_cpp/sleep_heartbeat.txt
legacy/src_cpp/sleep_events.txt
legacy/src_cpp/monitor.pid
legacy/src_python/*.lnk

# C++ Build outputs & intermediate files
legacy/src_cpp/sleep_monitor.exe
legacy/src_cpp/*.obj
legacy/src_cpp/*.ilk
legacy/src_cpp/*.pdb

# VS Code config (if any)
.vscode/

# Local config
config.json

# Claude Code project memory
.claude/

# src_slint runtime files（心拍/ロック等）
src_slint/data/device_heartbeat.txt
src_slint/data/sleep_heartbeat.txt
src_slint/data/monitor.pid
src_slint/data/monitor_paused
src_slint/data/generation.txt

# PC版のローカル自動バックアップ（日次、直近10世代。個人データを含むため追跡対象外）
src_slint/data/backups/
src_slint/data/last_auto_backup.txt

# 個人の睡眠・在宅/外出履歴データ（実データ。公開リポジトリに絶対に含めない）
src_slint/data/sleep_events.txt
src_slint/data/sleep_manual.txt
legacy/src_tauri/data/sleep_events.txt
legacy/src_tauri/data/sleep_manual.txt

# Android signing key (secret — never commit, any path incl. legacy/ backups)
**/*.keystore
